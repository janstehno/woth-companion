enum MapRegion {
  africa,
  alaska,
  idaho,
  newZealand,
  transylvania,
}

enum ProcessType {
  success,
  error,
  info,
}

enum HabitatType {
  primary,
  secondary,
}

enum ZoneType {
  eating,
  drinking,
  resting,
}

enum BuildingType {
  lodge,
  cabin,
  campsite,
  huntingStand,
}

enum MarkerType {
  fill,
  border,
}
